# Logs
logs
*.log
logs.txt
backend/logs.txt
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.development
.env.production
backend/.env
backend/.env.local

# Render CLI credentials (NEVER commit)
.render/
**/.render/

# Database backups (contienen datos personales reales - NEVER commit)
_supabase_backup_*/
*.dump

# Claude Code settings with credentials (NEVER commit)
.claude/mcp_settings.json
.claude/settings.local.json

# MCP Configuration (contains access tokens)
.mcp.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Hooks corregidos y funcionales

# Backups de migración con secretos del proyecto viejo
*.bak-old-project

# Codex CLI artifacts
.codex/
