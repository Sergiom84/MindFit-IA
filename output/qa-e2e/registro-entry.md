## 11.07.2026

- **E2E principiante Calistenia (registro → 8 semanas → historial/calendario/progreso)**: recorrido completo con usuaria QA nueva (Lucía Ferrero, id 1075) usando arnés Playwright headless móvil (`output/qa-e2e/`, con fecha de navegador desplazable y duraciones acortadas por intercepción de red). Plan manual principiante: 8 semanas × 3 sesiones (L-X-V) × 4 ejercicios; las 24 sesiones completadas (día 1 y sesión de sábado por UI; resto por API replicando las llamadas del reproductor). Verificadas pestañas: Calendario (semana correcta, patrón Empuje+Core / Tracción+Piernas / Empuje+Tracción, 3/3 series), Progreso (100%, desglose semanal, tiempo, logros) e Historial (totales y rutina completada). Intermedio (Elena) = 10 sem × 4 días y Avanzado (Diego) = 12 sem × 5 días verificados por API con filtro de lesiones OK (muñeca: cero apoyos de manos; codo: sin dominadas/fondos).
- **Fixes backend derivados del E2E**: (1) `db.js`: listener de `error` por cliente prestado del pool (el crash "Connection terminated unexpectedly" tumbaba el proceso entero; misma causa que los 500 de login en Render) + `server.js`: guardas `uncaughtException`/`unhandledRejection`. (2) `auth.js`: nuevo `POST /api/auth/refresh` (el frontend lo llamaba y el 404 degradaba a logout). (3) `plans.js /active-plan`: ya no regenera el calendario en cada día de descanso (borraba estados y re-anclaba semanas; llegó a inflar `workout_schedule` a 60 filas para 24 sesiones); solo genera si el plan no tiene calendario, y expone `generation_mode` para etiquetar "Fuente: Manual/IA" con honestidad. (4) `progress.js`: `GROUP BY` de estadísticas mensuales agrupaba por sesión (12 filas "July 2026 · 1 sesión") → agrupa por mes. (5) `music.js`: config por defecto en vez de 404 para usuarios sin `user_profiles`. (6) `persistSingleDaySession.js`: dedupe de sesión single-day pendiente por usuario/fecha/metodología (cada re-clic en "aceptar entrenamiento de hoy" creaba plan 'completed' + sesión duplicados; había 11 planes clonados). (7) `additionalControllers.saveSet`: guardado idempotente (doble tap/retry actualiza en vez de duplicar).
- **Fixes frontend derivados del E2E**: (1) `TodayTrainingTab.updateLocalState` aceptaba solo objetos pero 5 llamadas pasaban función (spread de función = no-op silencioso) → transiciones calentamiento→entrenamiento principal perdidas; ahora acepta ambas formas. (2) `WorkoutContext.loadActivePlan` + `RoutineScreen`: distinción `noPlan` vs error transitorio con reintento; un fallo de red ya no expulsa de Rutinas a Metodologías (desmontaba el reproductor en plena sesión). (3) `CalisteniaManualCard`: botón "Elegir Nivel Manualmente" también en el estado de error de la evaluación IA (antes, sin OpenAI válida la metodología quedaba bloqueada en "Reintentar"). (4) `ProgressTab`: logro "Semana Completa" nunca desbloqueaba por comparación estricta string/number. (5) `SeriesTrackingModal`: guard anti doble-guardado. (6) "Reanudar Entrenamiento" oculto cuando el día está 100% completado. (7) Etiqueta "Fuente del plan" honesta (Manual/IA) en vez de "OpenAI" hardcodeado.
- **Bugs pendientes documentados** (informe en sesión de QA): reanudar sesión con progreso parcial construye lista filtrada errónea y finaliza la sesión con 1/7 ejercicios; generador de `ensureScheduleV3` puede duplicar filas y anclar semana 1 en L-M-X al regenerar en fin de semana; confirmar plan en sábado sin startConfig produce 21/24 sesiones (semana 1 vacía); botón "Reanudar" con sesión completada no responde (guard silencioso); GIF con nombre de otro ejercicio en single-day (Flexión↔Pike); "Duración total: 0 min" en resumen del día; racha usa fecha real de completado; tarjetas de nivel de Calistenia sin rol de botón (accesibilidad); logout por inactividad puede saltar en mitad de una sesión activa. La clave OPENAI_API_KEY de `backend/.env` sigue inválida (401): la evaluación IA de nivel queda en fallback manual hasta reponerla.
